import downloadByUrl from "./downloadByUrl";
// const files = import.meta.glob('~/assets/docker/*.Dockerfile', { query: '?url', eager: true });

// function getFileUrl(fileName: string): string {
//  return files[`/assets/docker/${fileName}`] as string;
// }

// export default (fileName: string) => {
//  return downloadByUrl(getFileUrl(fileName), fileName)
// }